# configuration for Jupiter Juno forward and inversion calculation

# athena variables
set(NUMBER_GHOST_CELLS 2)
set(NVAPOR_VARIABLES 3)

# canoe variables
set(NCLOUDS 5)
set(NTRACER 3)
set(NCHEMISTRY 0)

# canoe configure
set(HYDROSTATIC ON)
set(NETCDF ON)
set(FITS ON)
set(RadianceSolver lambert)
